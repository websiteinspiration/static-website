import React from 'react'
import styled from 'styled-components'

const Guide = props => <Wrapper {...props} />

export default Guide

const Wrapper = styled.div`
  margin: 60px 0 30px 0;
  padding: 0 15px;
`
